# Syntecxhub_CSV_To_Excel_Converter_-CLI-Tool-
CSV → Excel Converter is a Python CLI tool that reads CSV files, cleans and normalizes data, handles missing values, parses dates, and exports structured .xlsx files using pandas and openpyxl. It supports column renaming via CLI flags and includes logging and error handling for reliable data processing.
